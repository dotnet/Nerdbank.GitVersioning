{
  "$schema": "src\\NerdBank.GitVersioning\\version.schema.json",
  "version": "1.6-beta",
  "assemblyVersion": {
    "precision": "revision"
  },
  "publicReleaseRefSpec": [
    // "^refs/heads/lkg$" All LKGs retain their full commit ID in the version, since we don't tag them.
  ],
  "cloudBuild": {
    "buildNumber": {
      "enabled": true
    }
  }
}
